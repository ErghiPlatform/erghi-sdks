<!--
  ChatFlow .NET SDK — Simulation / Integration Example
  Console application that exercises AIChatClient against http://localhost:5000

  dotnet run
-->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AssemblyName>ChatFlowSimulation</AssemblyName>
    <RootNamespace>ChatFlow.Simulation</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../AIChatSDK/ChatFlow.SDK.csproj" />
  </ItemGroup>

</Project>
